{% extends 'frontend/base.html.twig' %}
{% set active_page = 'home' %}
{% block description %}
    The home page for therecordbox . Quality second hand and new vinyl records.
{% endblock %}
{% block header %}
    <h1>Vinyl Store</h1>
{% endblock %}
{% block content %}
    <div class="row">
        <div class="col s12 m12">
            <div class="header-image">

            </div>
        </div>
    </div>

    {# the intro #}

    <div class="row homepage_sections">
        <div class="col s12 m12">
            <h1 class="flow-text">Online Vinyl shop for record enthusiasts.</h1>
            <p class="flow-text">Hello and welcome to my site. I stock an eclectic range of second hand and new records, hopefully you find something you
                like.</p>
            <p>I try and stick to the
                <a href="http://www.recordcollectorsguild.org/modules.php?op=modload&name=Sections&file=index&req=viewarticle&artid=17&page=1"
                   target="_blank">goldmine standard</a>
                when grading records however we have a grade for excellent similarly to
                <a href="https://thevinylfactory.com/features/a-comprehensive-guide-to-grading-vinyl-records/" target="_blank">this.</a>
                I also try to ensure that shipping rates <em>wont</em> be more expensive than the music you're buying.
            </p>
            <p>
                I use top quality mailers for packaging and enclose all sold vinyl in a plastic re-sealable sleeve so your vinyl will be well protected in
                transit.
            </p>
        </div>
    </div>

    {# the just added section #}

    <div class="row homepage_sections">

        <div class="col m12 s12">
            <h1 class="flow-text">Just added</h1>
        </div>
        {% if latest_releases is defined %}
            {% for latest in latest_releases %}
                <div class="col m3 s6">
                    <div class="card">
                        <div class="card-image">
                            <img class="activator" src="/uploads/{{ latest.image }}" alt="{{ latest.title|replace({' ':'_'}) }}_cover">
                        </div>
                        <div class="card-reveal">
                                 <span class="card-title white-text">
                                    <i class="material-icons right">close</i>
                                </span>
                            <div class="card-content white-text">
                                <p>{{ latest.artist }}</p>
                                <p>{{ latest.title }}</p>
                                <p>{{ latest.format }}</p>
                                <p class="hide-on-med-and-down">Media :{{ latest.media_condition }}</p>
                                <p class="hide-on-med-and-down">Sleeve :{{ latest.sleeve_condition }}</p>
                                <p>&euro;{{ latest.price }}</p>
                                <br>
                                <div class="divider"></div>
                                <a class="waves-effect waves-light"
                                   href="/vinyl/{{ latest.genre }}/release/{{ latest.title|slugify }}/{{ latest.id }}">Details</a>
                            </div>
                        </div>
                    </div>
                </div>

            {% endfor %}
        {% endif %}
    </div>


    <div class="spacer"></div>

    {# the random vinyl section #}

    <div class="row homepage_sections">
        <div class="col m12 s12">
            <h1 class="flow-text">Random Vinyl</h1>
        </div>
        {% if random_release is defined %}
            {% for random in random_release %}
                <div class="col m3 s6">
                    <div class="card">

                        <div class="card-image">
                            <img class="activator" src="/uploads/{{ random.image }}" alt="{{ random.title|replace({' ':'_'}) }}_cover">
                        </div>
                        <div class="card-reveal">
                                <span class="card-title white-text">
                                    <i class="material-icons right">close</i>
                                </span>
                            <div class="card-content white-text">
                                <p>{{ random.artist }}</p>
                                <p>{{ random.title }}</p>
                                <p>{{ random.format }}</p>
                                <p class="hide-on-med-and-down">Media :{{ random.media_condition|upper }}</p>
                                <p class="hide-on-med-and-down">Sleeve :{{ random.sleeve_condition|upper }}</p>
                                <p>&euro;{{ random.price }}</p>
                                <br>
                                <div class="divider"></div>
                                <a class="waves-effect waves-light"
                                   href="/vinyl/{{ random.genre }}/release/{{ random.title|slugify }}/{{ random.id }}">Details</a>
                            </div>
                        </div>

                    </div>
                </div>
            {% endfor %}
        {% endif %}

    </div>
    <div class="spacer"></div>

    <div class="row homepage_sections">
        <div class="col m12 s12">
            <h1 class="flow-text">Irish Artists</h1>
            <p class="flow-text">Got an album you'd like to sell? Get in touch to find out how we can stock your vinyl.</p>
        </div>

    </div>
{% endblock %}
