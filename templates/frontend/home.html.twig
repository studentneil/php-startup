{% extends 'frontend/base.html.twig' %}
{% set active_page = 'home' %}

{% block header %}
    <h1>Vinyl Store</h1>
{% endblock %}
{% block content %}
    <div class="row">
        <div class="col s12 m12">
            <div class="header-image">

            </div>
        </div>
        <div class="divider"></div>
        <div class="section">
            <div class="row center-align">
                <div class="col s12 m12">
                    <h1 class="flow-text">Vinylstore for record enthusiasts</h1>
                    <h2>Quality used vinyl at great prices.</h2>
                </div>
            </div>
        </div>
    </div>

    {#<div class="divider"></div>#}
    <div class="container center-align section-headings">
        <h1 class="flow-text">Featured Vinyls.</h1>
    </div>
    <div class="section card-panel">
        <div class="row center-align">


            <div class="col s12 m3">

            </div>
        </div>
    </div>
    {#<div class="divider"></div>#}
    <div class="container center-align section-headings">
    <h1 class="flow-text">New vinyl</h1>
    </div>


        <div class="row">


            {% if latest_releases is defined %}
                {% for latest in latest_releases %}
                    <div class="col m3 s6">
                        <div class="card">
                            <div class="card-image">
                                <img class="activator" src="/uploads/{{ latest.image }}" alt="">
                            </div>
                            <div class="card-reveal">
                                 <span class="card-title white-text">
                                    <i class="material-icons right">close</i>
                                </span>
                                <div class="card-content white-text">
                                    <strong>{{ latest.artist }}</strong> / <em>{{ latest.title }}</em>
                                    <br>
                                    &euro;{{ latest.price }}
                                    <div class="divider"></div>
                                    <a class="waves-effect waves-light" href="/release/{{ latest.id }}">Details</a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            {% endif %}

        </div>


    {#<div class="divider"></div>#}
    <div class="container center-align section-headings">
        <h1 class="flow-text">Random vinyl</h1>
    </div>
    <div class="row">




            {% if random_release is defined %}
                {% for random in random_release %}
                    <div class="col m3 s6">
                        <div class="card">

                            <div class="card-image">
                                <img class="activator" src="/uploads/{{ random.image }}" alt="">
                            </div>
                            <div class="card-reveal">
                                <span class="card-title white-text">
                                    <i class="material-icons right">close</i>
                                </span>
                                <div class="card-content white-text">
                                    <strong>{{ random.artist }}</strong> / <em>{{ random.title }}</em>
                                </div>
                            </div>

                        </div>
                    </div>
                {% endfor %}
            {% endif %}


        </div>
    </div>
{% endblock %}
