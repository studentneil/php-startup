{% extends 'frontend/base.html.twig' %}
{% set active_page = 'home' %}
{% block description %}
    Home page for the recordbox.ie Browse new and used vinyl.
{% endblock %}
{% block title %}Home{% endblock %}
{#{% block header %}#}
    {#<h1>Vinyl Store</h1>#}
{#{% endblock %}#}
{% block content %}
    <div class="row">
        {#<div class="col s12 m12">#}
            <div class="header-image hero_text">
                <h1 class="flow-text white-text">Online Vinyl shop for record collectors, selectors and enthusiasts.<br>
                    Shop for your next vinyl
                </h1>

            </div>
        {#</div>#}
    </div>

    {# the intro #}

    {#<div class="row homepage_sections">#}
        {#<div class="col s12 m12">#}

            {#<h2 class="flow-text">Featured Vinyl</h2>#}
        {#</div>#}
        {#<div class="col s12 m6">#}
            {#<div class="card horizontal">#}
                {#<div class="card-image">#}

                    {#<img src="{{ global.request.basepath }}/uploads/7c9ced597abea2ae8f370def8dd7da28.jpeg" alt="the_last_resort_pic">#}


                {#</div>#}
                {#<div class="card-stacked">#}
                    {#<div class="card-content featured_content">#}
                        {#<p>Trentemoller</p>#}
                        {#<p>The Last Resort</p>#}

                    {#</div>#}
                    {#<div class="card-action">#}
                        {#<a class="waves-effect waves-light" href="#">Details</a>#}
                    {#</div>#}
                {#</div>#}
            {#</div>#}
        {#</div>#}
        {#<div class="container">#}
        {#<div class="col s12 m12">#}

            {#<h2 class="flow-text">Welcome</h2>#}
        {#</div>#}
        {#<div class="col s12 m12">#}
            {#<p>Hello and welcome to The Record Box. I stock a pretty eclectic range of second hand and new records so hopefully you can find something you#}
                {#like.#}
            {#</p>#}
            {#<p>I try and stick to the goldmine standard when <a href="#gradingModal">grading records</a> and I also try to ensure that shipping rates <em>wont</em> be more expensive#}
                {#than the music you're buying.#}
            {#</p>#}
            {#<p>#}
                {#I also use top quality mailers for packaging and enclose all sold vinyl in a plastic re-sealable sleeve so your vinyl will be well protected in#}
                {#transit.#}
            {#</p>#}
        {#</div>#}
        {#</div>#}
    {#</div>#}


    {# the just added section #}

    <div class="row homepage_sections">

        <div class="col m12 s12">
            <h2 class="flow-text">Recently added</h2>
        </div>
        {% if latest_releases is defined %}
            {% for latest in latest_releases %}
                <div class="col m2 s6">
                    <div class="card">
                        <div class="card-image">
                            <img class="activator" src="/uploads/{{ latest.image }}" alt="{{ latest.title|replace({' ':'_'}) }}_cover">
                        </div>
                        <div class="card-reveal">
                                 <span class="card-title white-text">
                                    <i class="material-icons right">close</i>
                                </span>
                            <div class="card-content white-text">
                                <p>{{ latest.artist }}</p>
                                <p>{{ latest.title }}</p>
                                <p>{{ latest.format }}</p>
                                <p class="hide-on-med-and-down">Media :{{ latest.media_condition }}</p>
                                <p class="hide-on-med-and-down">Sleeve :{{ latest.sleeve_condition }}</p>
                                <p>&euro;{{ latest.price }}</p>
                                <br>
                                <div class="divider"></div>
                                <a class="waves-effect waves-light"
                                   href="/vinyl/{{ latest.genre }}/release/{{ latest.title|slugify }}/{{ latest.id }}">Details</a>
                            </div>
                        </div>
                    </div>
                </div>

            {% endfor %}
        {% endif %}
    </div>


    <div class="spacer"></div>

    {# the random vinyl section #}

    <div class="row homepage_sections">
        <div class="col m12 s12">
            <h2 class="flow-text">Random Vinyl</h2>
        </div>
        {% if random_release is defined %}
            {% for random in random_release %}
                <div class="col m2 s6">
                    <div class="card">

                        <div class="card-image">
                            <img class="activator" src="/uploads/{{ random.image }}" alt="{{ random.title|replace({' ':'_'}) }}_cover">
                        </div>
                        <div class="card-reveal">
                                <span class="card-title white-text">
                                    <i class="material-icons right">close</i>
                                </span>
                            <div class="card-content white-text">
                                <p>{{ random.artist }}</p>
                                <p>{{ random.title }}</p>
                                <p>{{ random.format }}</p>
                                <p class="hide-on-med-and-down">Media :{{ random.media_condition|upper }}</p>
                                <p class="hide-on-med-and-down">Sleeve :{{ random.sleeve_condition|upper }}</p>
                                <p>&euro;{{ random.price }}</p>
                                <br>
                                <div class="divider"></div>
                                <a class="waves-effect waves-light"
                                   href="/vinyl/{{ random.genre }}/release/{{ random.title|slugify }}/{{ random.id }}">Details</a>
                            </div>
                        </div>

                    </div>
                </div>
            {% endfor %}
        {% endif %}

    </div>
    <div class="spacer"></div>

    <div class="row homepage_sections">
        <div class="col m12 s12">
            <h2 class="flow-text">Irish Artists</h2>
            <p class="flow-text">Got an album you'd like to sell? Get in touch to find out how we can stock your vinyl.</p>
        </div>

    </div>
{% endblock %}
